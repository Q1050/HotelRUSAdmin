import{Head,Link,router}from'@inertiajs/react';import{AdminLayout}from'@/components/layout/AdminLayout';import{Button}from'@/components/ui/button';import{Download,WalletCards}from'lucide-react';type F={id:number;number:string;reservationId:number;reference:string;guestName:string;currency:string;status:string;charges:number;payments:number;refunds:number;balance:number;updatedAt:string|null};
export default function Index({folios,status}:{folios:F[];status:string}){return <AdminLayout title="Guest folios"><Head title="Guest folios"/><div className="space-y-6"><header className="flex flex-wrap justify-between gap-3"><div><h1 className="text-2xl font-bold">Guest folios</h1><p className="text-gray-600">Charges, payments, refunds and outstanding guest balances.</p></div><Button variant="outline" asChild><a href={route('dashboard.folios.reconciliation')}><Download className="mr-2" size={16}/>Reconciliation CSV</a></Button></header><div className="flex gap-2 rounded-xl border bg-white p-3">{['open','settled','all'].map(s=><button key={s} onClick={()=>router.get(route('dashboard.folios.index'),{status:s})} className={`rounded-lg px-4 py-2 text-sm capitalize ${status===s?'bg-indigo-600 text-white':'bg-gray-100 text-gray-600'}`}>{s}</button>)}</div>{folios.length?<div className="overflow-hidden rounded-xl border bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full divide-y"><thead className="bg-gray-50"><tr>{['Folio','Guest','Charges','Payments','Refunds','Balance','Status'].map(h=><th key={h} className="px-4 py-3 text-left text-xs uppercase text-gray-500">{h}</th>)}</tr></thead><tbody className="divide-y">{folios.map(f=><tr key={f.id}><td className="px-4 py-3"><Link className="font-mono text-indigo-600 hover:underline" href={route('dashboard.folios.show',f.id)}>{f.number}</Link><div className="text-xs text-gray-500">{f.reference}</div></td><td className="px-4 py-3 font-medium">{f.guestName}</td><Money n={f.charges} c={f.currency}/><Money n={f.payments} c={f.currency}/><Money n={f.refunds} c={f.currency}/><td className={`px-4 py-3 font-semibold ${f.balance>0?'text-red-700':'text-emerald-700'}`}>{money(f.balance,f.currency)}</td><td className="px-4 py-3 capitalize">{f.status}</td></tr>)}</tbody></table></div></div>:<div className="rounded-xl border bg-white p-12 text-center"><WalletCards className="mx-auto text-indigo-500" size={36}/><h2 className="mt-3 font-semibold">No {status==='all'?'':status} folios</h2><p className="text-sm text-gray-500">Open a reservation and select its folio to start posting charges.</p></div>}</div></AdminLayout>}function Money({n,c}:{n:number;c:string}){return <td className="px-4 py-3">{money(n,c)}</td>}const money=(n:number,c:string)=>new Intl.NumberFormat(undefined,{style:'currency',currency:c}).format(n);
