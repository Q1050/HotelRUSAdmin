import { PlatformLayout } from '@/components/layout/PlatformLayout';
import { Activity as ActivityIcon, Building2, Clock, ShieldCheck, UserRound } from 'lucide-react';
type Event={id:number;action:string;description:string;hotel:string;actor:string;severity:string;occurredAt:string};
export default function Activity({events}:{events:Event[]}){return <PlatformLayout><div className="mb-7"><p className="text-xs font-bold uppercase tracking-[0.16em] text-indigo-600">Security trail</p><h1 className="mt-2 text-3xl font-bold">Platform activity</h1><p className="mt-2 max-w-2xl text-sm text-slate-500">Sensitive platform-owner actions across client hotels, retained for support and accountability.</p></div><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-100 p-5"><div className="flex items-center gap-3"><div className="rounded-xl bg-indigo-50 p-2.5 text-indigo-700"><ShieldCheck size={19}/></div><div><h2 className="font-bold">Audit timeline</h2><p className="text-xs text-slate-500">Latest {events.length} recorded actions</p></div></div></div><div className="divide-y divide-slate-100">{events.map(event=><article key={event.id} className="grid gap-3 p-5 transition hover:bg-slate-50/70 md:grid-cols-[auto_1fr_auto]"><div className="rounded-xl bg-slate-100 p-2.5 text-slate-500 md:self-start"><ActivityIcon size={18}/></div><div><p className="text-sm font-semibold text-slate-800">{event.description}</p><div className="mt-2 flex flex-wrap gap-x-4 gap-y-1 text-xs text-slate-500"><span className="flex items-center gap-1"><Building2 size={13}/>{event.hotel}</span><span className="flex items-center gap-1"><UserRound size={13}/>{event.actor}</span><span className="flex items-center gap-1"><Clock size={13}/>{new Date(event.occurredAt).toLocaleString()}</span></div></div><span className="h-fit rounded-full bg-amber-50 px-2.5 py-1 text-xs font-semibold capitalize text-amber-700">{event.severity}</span></article>)}{events.length===0&&<div className="px-5 py-16 text-center"><ShieldCheck className="mx-auto text-slate-300" size={36}/><p className="mt-3 text-sm text-slate-500">No platform activity recorded yet.</p></div>}</div></section></PlatformLayout>}
