<?php
use Illuminate\Database\Migrations\Migration;use Illuminate\Database\Schema\Blueprint;use Illuminate\Support\Facades\Schema;
return new class extends Migration{public function up():void{Schema::table('plans',function(Blueprint$t){$t->string('stripe_price_id')->nullable()->unique();$t->unsignedInteger('monthly_price')->nullable();});Schema::create('billing_webhook_events',function(Blueprint$t){$t->id();$t->string('provider')->default('stripe');$t->string('provider_event_id')->unique();$t->string('type');$t->string('status')->default('processing');$t->text('error')->nullable();$t->timestamp('processed_at')->nullable();$t->timestamps();});}public function down():void{Schema::dropIfExists('billing_webhook_events');Schema::table('plans',fn(Blueprint$t)=>$t->dropColumn(['stripe_price_id','monthly_price']));}};
